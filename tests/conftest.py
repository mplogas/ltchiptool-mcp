"""Shared pytest configuration for ltchiptool-mcp tests.

asyncio_mode is set to "auto" in pyproject.toml so async tests do not
need per-function decorators. Add shared fixtures here as the suite grows.
"""
